# Docker - Commands

To run docker without sudo

```bash
sudo usermod -aG docker $USER
newgrp docker #Refresh the terminal
```

## Images

List local images

```bash
	sudo docker images
	docker images
```
